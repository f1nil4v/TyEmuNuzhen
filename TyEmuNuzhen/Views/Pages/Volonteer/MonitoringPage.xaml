<Page x:Class="TyEmuNuzhen.Views.Pages.MonitoringPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TyEmuNuzhen.Views.Pages"
      mc:Ignorable="d" 
      Title="MonitoringPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Мониторинг" FontSize="24" Margin="10,10,0,0"/>
        <Label Grid.Row="1" x:Name="lbl" Content="Детей нет" FontSize="24" Margin="10,10,0,0" Visibility="Hidden"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="childrenContainer" Orientation="Vertical">
                
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Bottom" CornerRadius="1000" Background="#A3B92E" Margin="0,0,30,30">
            <Button x:Name="addChildBtn" Padding="0,0,0,0" Background="{x:Null}" BorderBrush="{x:Null}" BorderThickness="0" Height="50" Width="50" Click="addChildBtn_Click">
                <StackPanel>
                    <Image Source="/Images/Controls/add.png" Height="30" Width="30"></Image>
                </StackPanel>
            </Button>
        </Border>
    </Grid>
</Page>
